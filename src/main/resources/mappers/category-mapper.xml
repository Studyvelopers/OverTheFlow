<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" " http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.studyveloper.overtheflow.mapper.CategoryMapper">
	<resultMap id="category" type="CategoryVO">
		<id property="id" column="category_id"/>
		<result property="name" column="category_name"/>
		<result property="superId" column="category_super_id"/>
		<result property="memberId" column="member_id"/>
	</resultMap>

	<insert id="addCategory" parameterType="CategoryVO">
		INSERT INTO 
			category (category_id, category_name, category_super_id, member_id)
		VALUES
			(#{id}, #{name}, #{superId}, #{memberId})
	</insert>

	<update id="modifyCategory" parameterType="CategoryVO">
		UPDATE
			category
		SET
			category_name = #{name}
			category_super_id = #{superId},
			member_id = #{memberId}
		WHERE
			category_id = #{id}
	</update>

	<delete id="deleteCategories" parameterType="Map">
		DELETE FROM
			category
		<!-- 검색 -->
		<if test="searchOptions != null">
		<if test="searchOptions.size gt 0">
		<where>
			<foreach collection="searchOptions" item="searchOption" index="index" separator=" ">
				${searchOption.conjunction}
				${searchOption.columnName}
				<if test="searchOption.compareType lt 0">
				IN
					<foreach collection="searchOption.keywords" item="keyword" index="index" open="(" separator="," close=")">
						#{keyword}
					</foreach>
				</if>
				<if test="searchOption.compareType == 0">
					= #{searchOption.keyword}
				</if>
				<if test="searchOption.compareType gt 0">
					LIKE CONCAT('%', #{searchOption.keyword}, '%')
				</if>
			</foreach>
		</where>
		</if></if>
	</delete>

	<delete id="deleteCategory" parameterType="String">
		DELETE FROM
			category
		WHERE
			category_id = #{id}
	</delete>

	<select id="searchCategory" parameterType="String" resultMap="category">
		SELECT * FROM
			category
		WHERE
			category_id = #{categoryId}
	</select>
	
	<select id="searchCategories" parameterType="Map" resultMap="category">
		SELECT * FROM
			category
		<!-- 검색 -->
		<if test="searchOptions != null">
		<if test="searchOptions.size gt 0">
		<where>
			<foreach collection="searchOptions" item="searchOption" index="index" separator=" ">
				${searchOption.conjunction}
				${searchOption.columnName}
				<if test="searchOption.compareType lt 0">
				IN
					<foreach collection="searchOption.keywords" item="keyword" index="index" open="(" separator="," close=")">
						#{keyword}
					</foreach>
				</if>
				<if test="searchOption.compareType == 0">
					= #{searchOption.keyword}
				</if>
				<if test="searchOption.compareType gt 0">
					LIKE CONCAT('%', #{searchOption.keyword}, '%')
				</if>
			</foreach>
		</where>
		</if>
		</if>
		
		<!-- 정렬 -->
		<if test = "sortingOptions != null">
			<if test="sortingOptions.size gt 0">
			ORDER BY 
			<foreach collection="sortingOptions" item="sortingOption" separator=",">
				${sortingOption.columnName}
				<choose>
					<when test="sortingOption.ordering">
						DESC
					</when>
					<otherwise>
						ASC
					</otherwise>
				</choose> 
			</foreach>
			</if>
		</if>
		
		<!-- 페이징 -->
		<if test="pagingOption != null">
			<if test="pagingOption.size gt 0">
				LIMIT
				<if test="pagingOption.offset gt 0">
					#{pagingOption.offset},
				</if>
				#{pagingOption.size}
			</if>
		</if>		
	</select>
	
	<select id="getCategorySize" parameterType="Map" resultMap="category">
		SELECT COUNT(*) FROM
			category
		<!-- 검색 -->
		<if test="searchOptions != null">
		<if test="searchOptions.size gt 0">
		<where>
			<foreach collection="searchOptions" item="searchOption" index="index" separator=" ">
				${searchOption.conjunction}
				${searchOption.columnName}
				<if test="searchOption.compareType lt 0">
				IN
					<foreach collection="searchOption.keywords" item="keyword" index="index" open="(" separator="," close=")">
						#{keyword}
					</foreach>
				</if>
				<if test="searchOption.compareType == 0">
					= #{searchOption.keyword}
				</if>
				<if test="searchOption.compareType gt 0">
					LIKE CONCAT('%', #{searchOption.keyword}, '%')
				</if>
			</foreach>
		</where>
		</if>
		</if>	
	</select>
</mapper> 